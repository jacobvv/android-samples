<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.jacobvv.androidsamples">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".permission.sample.CommonActivity"></activity>
        <activity android:name=".permission.sample.MultiActivity" />
        <activity android:name=".permission.sample.SimpleActivity" />
        <activity android:name=".permission.PermissionMainActivity" />
        <activity android:name=".recycler.cursor.CursorActivity" />
        <activity android:name=".recycler.multi.MultiTypeActivity" />
        <activity android:name=".recycler.single.SingleTypeActivity" />
        <activity android:name=".recycler.RecyclerActivity" />
        <activity android:name=".databus.ActivityB" />
        <activity android:name=".databus.ActivityA" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>